language: node_js
cache:
  yarn: true
  directories:
  - "node_modules"
notifications:
  email: false
node_js:
- stable
os:
- linux
addons:
  chrome: stable
  apt:
    sources:
    - sourceline: "deb https://dl.yarnpkg.com/debian/ stable main"
      key_url: "https://dl.yarnpkg.com/debian/pubkey.gpg"
    packages:
    - yarn
install:
- git clone https://github.com/ngageoint/opensphere-yarn-workspace.git
- cd opensphere-yarn-workspace/workspace
- git clone https://github.com/ngageoint/opensphere.git
- git clone https://github.com/ngageoint/opensphere-app-example.git
- yarn config list
- yarn
- cd opensphere-app-example
script:
- set -e
- yarn run build
- set +e
